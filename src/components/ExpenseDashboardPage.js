import React from 'react';

const ExpenseDashboardPage = (props) =>{

    console.log(props);
    // Also use this example http://localhost:8080/?query?=rent&sort=date "see in location key"
    return (
        <div>
            This is Dashboard
        </div>
    );
};


export default ExpenseDashboardPage;